version: '3.8'

services:
  web:
    build: .
    command: /bin/sh -lc "python manage.py collectstatic --noinput && gunicorn ycleaning.wsgi:application --bind 0.0.0.0:8000"
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on: []
